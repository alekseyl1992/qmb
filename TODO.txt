//плюсом помечены выполненные пункты
//воскл. знаком - важные

- 4) сделать ломаные стрелочки
- 8) продумать организацию подмоделей, или просто отказаться от их реализации
- 22) написать свою модель для лога данных с блэкджэком и.. нормальным интерфейсом и сигналом на автоскролл
- 38) управление активностью undoAction/redoAction
- 39) привести в порядок взаимоотношения MainWindow и Document
- 40) свойства элементов
- 42) issue: если после alignToGrid() перейти на вкладку с кодом, а затем на сцену - вызовется fillModel()
- 43) Рефакторинг: привести все слоты к единому стилю вида onSomeEvent()
- 45) реализация исключений при ошибках парсинга XML
- 46) дописать Validator - compile-time часть
- 47) добавить возможность удаления модели из списка последних открытых
- 48) когда должен обновляться интерфейс: при нажатии кнопки или при приходе события (play/stop etc)

пост, пока не вострбован
Qt 4.8.0, MinGW 4.6.3, Windows 7

[CODE=cpp]
QString Validator::checkSyntax(const QString &script)
{
    QScriptEngine eng;
    auto result = eng.checkSyntax(script);

    return result.errorMessage();
}
[/CODE]
передаю туда, например
[CODE=js]output = randID([/CODE]
где явно отсутсвует закрывающая скобка
на выходе
state == Error
номер строки и столбца не равны нулю
а вот errorMessage - пустая строка...

Попробовал отключить установку транслятора qt_ru.qm, не помогло..
Впринципе это не очень критично

Added:
лол, оно ещё и работает в таком виде: то есть реально вызывается функция randID()
но если вместо скрипта написать абракадабру, то ошибка выдаётся
